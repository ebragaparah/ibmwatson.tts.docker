version: '3.3'

services:
  db:
    image: mysql:5.7
    volumes:
      - db_data:/var/lib/mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: abcdefghij
      MYSQL_DATABASE: commentsuniverse
      MYSQL_USER: abcdefghij
      MYSQL_PASSWORD: abcdefghij
volumes:
  db_data: {}